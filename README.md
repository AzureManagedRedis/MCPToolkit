# MCPToolkit

This repository serves as a central hub for all MCP (Model Context Protocol) related resources for Azure Managed Redis (AMR). 
Currently, it includes:

- Links to remote Azure Managed Redis MCP server repositories
- Demo samples of Foundry agent that uses Azure Managed Redis as memory store and vector store through remote MCP server.
